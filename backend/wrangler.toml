# Cloudflare Workers 配置文件
name = "gold-price-monitor-api"
main = "src/index.js"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# 环境变量配置（生产环境请使用 wrangler secret 设置）
# MODELSCOPE_API_KEY = "your-api-key"

# 开发环境配置
[dev]
port = 8787
local_protocol = "http"
